import React from "react";
import style from './createUser.module.scss';
import {useParams} from "react-router-dom";

export const CreateUser = () => {

    return (
        <p className={style.createUser}>
            CreateUser
        </p>
    )
};